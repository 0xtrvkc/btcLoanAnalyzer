{
  "name": "btc-loan-analyzer-export",
  "private": true,
  "version": "2.0.0",
  "description": "BTC loan scenario terminal with a shared calculation and report engine",
  "scripts": {
    "export": "node scripts/export-position.js",
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-static.js",
    "export:offline": "node scripts/export-position.js --offline"
  },
  "dependencies": {
    "puppeteer": "^25.0.0"
  }
}
